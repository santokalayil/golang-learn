package functions

// capitalized functions are only exported
func ParmeterizedFunction(x int, y int) int {
	return x + y
}

// the following is also possible
// func myFunction(x int, y int) (result int) {
